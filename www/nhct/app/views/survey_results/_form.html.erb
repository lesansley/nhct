<%= form_for(@survey_result) do |f| %>
  <% if @survey_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_result.errors.count, "error") %> prohibited this survey_result from being saved:</h2>

      <ul>
      <% @survey_result.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ResultValue %><br>
    <%= f.number_field :ResultValue %>
  </div>
  <div class="field">
    <%= f.label :SurveyDate %><br>
    <%= f.date_select :SurveyDate %>
  </div>
  <div class="field">
    <%= f.label :operation_id %><br>
    <%= f.text_field :operation_id %>
  </div>
  <div class="actions">
    <%= f.submit "Finish" %>
  </div>
<% end %>
